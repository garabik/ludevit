# Automatically added by dh_pycentral
rm -f /var/lib/pycentral/ludevit.pkgremove
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall ludevit
	if grep -qs '^ludevit$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^ludevit$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
